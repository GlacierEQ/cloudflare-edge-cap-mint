[project]
name = "cloudflare-edge-cap-mint"
version = "0.0.1"
description = "Scaffold: Edge Cap Mint (independent Cloudflare lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
